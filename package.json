{
  "name": "taskly",
  "private": true,
  "version": "1.0.0",
  "description": "Taskly — full-stack todo app (React + Express + MongoDB)",
  "scripts": {
    "dev": "concurrently -n api,web -c green,cyan \"npm --prefix api run dev\" \"npm --prefix web run dev\"",
    "test": "npm --prefix api test && npm --prefix web test",
    "postinstall": "npm --prefix api install && npm --prefix web install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
